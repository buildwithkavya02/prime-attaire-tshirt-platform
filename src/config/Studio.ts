/**
 * Central place for studio-wide configuration values.
 * Previously the WhatsApp number was hardcoded inside Customize.tsx.
 * Keeping it here means it only needs to change in one place.
 */
export const STUDIO_WHATSAPP_NUMBER = "9962605619";